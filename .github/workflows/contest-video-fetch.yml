name: Contest Video Fetch Bridge

on:
  push:
    branches: [main]
    paths:
      - 'contest-sync/request.json'

permissions:
  contents: read
  id-token: write

env:
  BRIDGE_URL: https://wtqcolceuvlxrelugvjw.supabase.co/functions/v1/contest-video-bridge
  OIDC_AUDIENCE: madger-contest-video-bridge

jobs:
  fetch-video:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read request
        id: req
        shell: bash
        run: |
          action=$(jq -r '.action // "video"' contest-sync/request.json)
          echo "action=$action" >> "$GITHUB_OUTPUT"
          if [[ "$action" == "list_ready" || "$action" == "fetch_jwks" ]]; then
            exit 0
          fi
          if [[ "$action" != "video" ]]; then
            echo 'Invalid action' >&2
            exit 1
          fi
          submission_id=$(jq -r '.submission_id' contest-sync/request.json)
          file_name=$(jq -r '.file_name' contest-sync/request.json)
          if [[ -z "$submission_id" || "$submission_id" == "null" || -z "$file_name" || "$file_name" == "null" ]]; then
            echo 'Invalid request.json' >&2
            exit 1
          fi
          echo "submission_id=$submission_id" >> "$GITHUB_OUTPUT"
          echo "file_name=$file_name" >> "$GITHUB_OUTPUT"

      - name: Get GitHub OIDC token
        if: steps.req.outputs.action != 'fetch_jwks'
        id: oidc
        shell: bash
        run: |
          token_json=$(curl -fsSL --max-time 20 -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=$OIDC_AUDIENCE")
          token=$(printf '%s' "$token_json" | jq -r '.value')
          test -n "$token" && test "$token" != "null"
          echo "token=$token" >> "$GITHUB_OUTPUT"

      - name: Fetch secure transfer payload
        id: fetch
        shell: bash
        env:
          OIDC_TOKEN: ${{ steps.oidc.outputs.token }}
          ACTION: ${{ steps.req.outputs.action }}
          SUBMISSION_ID: ${{ steps.req.outputs.submission_id }}
          FILE_NAME: ${{ steps.req.outputs.file_name }}
        run: |
          mkdir -p contest-sync/out
          if [[ "$ACTION" == "fetch_jwks" ]]; then
            curl --fail --location --silent --show-error --max-time 20 \
              https://token.actions.githubusercontent.com/.well-known/jwks \
              --output contest-sync/out/github-actions-jwks.json
            jq -e '.keys | type == "array" and length > 0' contest-sync/out/github-actions-jwks.json >/dev/null
            echo "artifact_name=contest-github-jwks" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          CURL=(curl --fail --location --silent --show-error --max-time 30 --retry 2 --retry-all-errors --retry-delay 3 -H "Authorization: Bearer $OIDC_TOKEN")
          if [[ "$ACTION" == "list_ready" ]]; then
            "${CURL[@]}" "$BRIDGE_URL?action=list_ready" --output contest-sync/out/ready.json
            jq -e '.rows | type == "array"' contest-sync/out/ready.json >/dev/null
            echo "artifact_name=contest-ready-submissions" >> "$GITHUB_OUTPUT"
          else
            "${CURL[@]}" "$BRIDGE_URL?action=video&submission_id=$SUBMISSION_ID" --output "contest-sync/out/$FILE_NAME"
            test -s "contest-sync/out/$FILE_NAME"
            sha256sum "contest-sync/out/$FILE_NAME" | tee contest-sync/out/SHA256.txt
            echo "artifact_name=contest-video-$SUBMISSION_ID" >> "$GITHUB_OUTPUT"
          fi

      - name: Upload transfer artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.fetch.outputs.artifact_name }}
          path: contest-sync/out/
          if-no-files-found: error
          retention-days: 1
          compression-level: 0
