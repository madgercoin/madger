# Approved MADGER social assets

Store only final, approved campaign media here. Every asset must comply with `MADGER_IDENTITY_LOCK.md`; rejected or off-model Madger artwork must never be added.

Use stable filenames. The GitHub raw-file URL can be supplied to the Buffer publisher as the public HTTPS asset URL.
