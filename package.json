{
  "name": "madger-website",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "description": "Official MADGER website",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "wrangler deploy",
    "check": "npm run build && wrangler deploy --dry-run",
    "validate:js": "node --check build.mjs && node --check script.js && node --check launch-state.js && node --check site-config.mjs && node --check scripts/validate-site.mjs && node --check scripts/validate-dist.mjs",
    "validate:site": "node scripts/validate-site.mjs",
    "validate:dist": "node scripts/validate-dist.mjs",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "test:buffer": "node --test test/buffer-automation.test.mjs",
    "test:performance": "node --test test/buffer-performance.test.mjs",
    "validate": "npm run validate:js && npm run test:buffer && npm run test:performance && npm run validate:launch && npm run validate:site && npm run validate:dist && npm run scan:secrets",
    "check:deployment": "node scripts/check-deployment.mjs",
    "validate:launch": "node scripts/validate-launch.mjs",
    "social:post": "node src/index.js"
  },
  "devDependencies": {
    "wrangler": "4.120.1"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
